# Dev/test dependencies for the cross-platform backend test suite.
# Run:  pip install -r requirements-test.txt  &&  pytest
pytest>=8,<10
moto[dynamodb]>=5,<6
boto3>=1.34

# runtime deps required to import the Lambda handlers outside AWS
# (mirrors server/shared/layers/requirements.txt + service requirements)
aws-lambda-powertools[tracer]>=3,<4
jsonpickle>=4,<5
simplejson>=3.19,<5
aws-requests-auth>=0.4,<0.5
