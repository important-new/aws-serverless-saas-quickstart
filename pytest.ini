[pytest]
# Cross-platform backend test suite (pytest + moto). Runs on Windows/macOS/Linux
# with no Docker / no AWS account:  pytest
testpaths = server/services
python_files = test_*.py
python_functions = test_*
addopts = -ra -q
